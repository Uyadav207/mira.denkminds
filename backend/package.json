{
	"name": "backend",
	"scripts": {
		"start": "bun run src/index.ts",
		"dev": "bun run --watch src/index.ts",
		"prepare": "husky",
		"check": "biome check ./src "
	},
	"dependencies": {
		"hono": "^4.6.12"
	},
	"devDependencies": {
		"@biomejs/biome": "1.9.4",
		"@types/bun": "latest",
		"husky": "^9.1.7"
	}
}
