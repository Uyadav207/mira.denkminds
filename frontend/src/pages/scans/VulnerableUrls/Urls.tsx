import Url from "../../../components/scans/Url"; // Adjust the import path based on your project structure

const Urls: React.FC = () => {
	return (
		<div className="container mx-auto p-4">
			<Url />
		</div>
	);
};

export default Urls;
